{% extends 'base.html.twig' %}

{% block title %}{{ software.editor.libelle }}{% endblock %}

{% block body %}
    <h1>{{ software.editor.libelle }}</h1>
    <p>{{ software.editor.description }}</p>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ software.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ software.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('application_software_index') }}">back to list</a>

    <a href="{{ path('application_software_edit', {'id': software.id}) }}">edit</a>

    {{ include('application/software/_delete_form.html.twig') }}
{% endblock %}
