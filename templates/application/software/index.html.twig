{% extends 'base.html.twig' %}

{% block title %}Liste des logiciels{% endblock %}

{% block body %}
    <h1>Liste des logiciels</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for software in softwares %}
            <tr>
                <td>{{ software.id }}</td>
                <td>{{ software.libelle }}</td>
                <td>
                    <a href="{{ path('application_software_show', {'id': software.id}) }}">show</a>
                    <a href="{{ path('application_software_edit', {'id': software.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('application_software_new') }}">Create new</a>
{% endblock %}
